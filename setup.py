from setuptools import setup, find_packages

setup(
    name="QFNUCLASSSELECTOR",
    version="1.4.2",
    packages=find_packages(include=['log', 'json', 'WebUI', 'Module', 'Essence']),
    install_requires=[]
)